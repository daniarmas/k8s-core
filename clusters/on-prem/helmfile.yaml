helmfiles:
- path: ../../apps/cilium/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/cert-manager/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/longhorn/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/vault/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/vault-secrets-operator/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/crunchy-postgres-operator/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/cnpg/helmfile.yaml
  values:
  - environment: on-prem