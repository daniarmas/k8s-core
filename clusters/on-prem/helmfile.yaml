helmfiles:
- path: ../../apps/cilium/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/cert-manager/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/longhorn/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/vault/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/vault-secrets-operator/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/cnpg/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/ingress-nginx/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/metallb/helmfile.yaml
  values:
  - environment: on-prem

- path: ../../apps/ot-redis-operator/helmfile.yaml
  values:
  - environment: on-prem