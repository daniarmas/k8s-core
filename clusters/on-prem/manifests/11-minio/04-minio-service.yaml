# Update the MinIO API service to use HTTPS
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: minio-tenant
spec:
  ports:
  - port: 443      # ✅ HTTPS port for API
    targetPort: 9000
    protocol: TCP
    name: https-api
  selector:
    app: minio