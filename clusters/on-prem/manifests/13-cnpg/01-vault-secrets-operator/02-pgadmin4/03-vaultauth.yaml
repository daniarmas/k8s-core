apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: default
  namespace: harbor
spec:
  # Address of your Vault server
  address: "http://vault.vault.svc.cluster.local:8200"
  # Skip TLS verification (set to true if using HTTP or self-signed certs)
  skipTLSVerify: true
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: harbor
spec:
  # Reference to the VaultConnection in the same namespace
  vaultConnectionRef: default
  
  # Kubernetes authentication method
  method: kubernetes
  mount: kubernetes
  
  kubernetes:
    # Role configured in Vault for harbor-pgadmin4
    role: "harbor-pgadmin4"
    
    # Service account harbor-pgadmin4 uses
    serviceAccount: "harbor-pgadmin4"