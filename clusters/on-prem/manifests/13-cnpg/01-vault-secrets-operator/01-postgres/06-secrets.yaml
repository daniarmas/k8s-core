apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: core-api-postgres-secret
  namespace: core-api
spec:
  # Reference to VaultConnection (uses default if not specified)
  vaultAuthRef: core-api-postgres
  
  # Path to secret in Vault
  mount: secret
  type: kv-v2
  path: core-api/postgres
  
  # Kubernetes secret to create/update
  destination:
    name: core-api-postgres
    create: true
    type: Opaque
  
  # Refresh the secret every 5 minutes
  refreshAfter: 5m
  
  # Rollout restart deployments when secret changes
  # rolloutRestartTargets:
  #   - kind: Deployment
  #     name: pgadmin4